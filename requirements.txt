# Core LLM agent dep
openai>=1.30.0

# Used by core/engine.py strategies (standard library covers most, but explicit for deploys)
# No extra deps needed beyond stdlib + openai for the trading engine itself

# Optional: faster JSON parsing
# orjson>=3.9.0

# Optional: dotenv loading
python-dotenv>=1.0.0

# Optional: Telegram via httpx instead of curl
# httpx>=0.27.0
